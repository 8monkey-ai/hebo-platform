{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["NODE_ENV", "VITE_"],
      "outputs": ["**/generated/**", ".react-router/types/**", "build/**", "dist/**", "out/**"]
    },
    "predev": {
      "outputs": ["**/generated/**", ".react-router/types/**"]
    },
    "dev": {
      "dependsOn": ["^predev", "predev"],
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "lint": {
      "dependsOn": ["^predev", "predev"],
      "outputs": []
    }
  }
}
