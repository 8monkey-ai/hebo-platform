{
  "name": "@hebo/auth",
  "version": "0.0.1",
  "private": true,
  "sideEffects": false,
  "engines": {
    "bun": "^1.3.1"
  },
  "type": "module",
  "scripts": {
    "build": "bun run db generate && bun build src/index.ts --compile --minify-whitespace --minify-syntax --target=bun --outfile dist/server",
    "clean": "git clean -fdx -e '.env*' .",
    "db": "prisma",
    "dev": "bun --hot src/index.ts | pino-pretty",
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "prestart": "bun run build",
    "start": "./dist/server"
  },
  "dependencies": {
    "@bogeychan/elysia-logger": "^0.1.10",
    "@elysiajs/cors": "catalog:",
    "@elysiajs/opentelemetry": "catalog:",
    "@hebo/shared-api": "workspace:*",
    "@prisma/adapter-pg": "^7.1.0",
    "@prisma/client": "^7.1.0",
    "@upyo/core": "^0.3.2",
    "@upyo/smtp": "^0.3.2",
    "better-auth": "^1.4.5",
    "elysia": "catalog:",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@hebo/typescript-config": "workspace:*",
    "@electric-sql/pglite": "^0.3.7",
    "@types/pg": "^8.15.5",
    "bun-types": "^1.3.1",
    "pino-pretty": "^13.1.1",
    "prisma": "^7.1.0"
  }
}
