#!/bin/sh
HEBO_MODE="${HEBO_MODE:-standalone}"
if [ "${HEBO_MODE}" != "standalone" ]; then
  echo "[svc-console] Skipped (HEBO_MODE=${HEBO_MODE})"
  exec s6-svc -Od .
fi
echo "[svc-console] Starting Console on :8520"
cd /app/console
exec bun run vite preview --host 0.0.0.0 --port 8520
